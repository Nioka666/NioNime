/* eslint-disable @typescript-eslint/no-unused-vars */
import chalk from "chalk";

export const greenBold = (text) => {
  return chalk.green.bold(text);
};

export const redBold = (text) => {
  return chalk.red.bold(text);
};
